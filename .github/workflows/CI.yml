name: BuildAndTest

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    #environment: Release
    
    strategy:
      matrix:
        os: [macOS-latest, ubuntu-latest, windows-latest]
    
    runs-on: ${{ matrix.os }}
        
    steps:
    - name: Checkout repository
    - uses: actions/checkout@v2
